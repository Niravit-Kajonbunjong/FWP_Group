<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="/css/bootstrap.min.css" rel="stylesheet">
    <script src="/js/bootstrap.bundle.min.js"></script>
    <link rel="stylesheet" href="/css/index.css">
    
    <style>
        body {
            background-color: #ffffff; 
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            margin: 0;
        }

        .login-container {
            width: 600px;
            height: 400px;
            background-image: url('/img/bgpink.svg'); 
            background-size: contain;
            background-repeat: no-repeat;
            background-position: center;
            position: relative;
        }

        .btn-pink {
            background-color: #ffaeee;
            color: #1e1e1e;
            border: 1px solid #ffaeee;
            transition: 0.3s;
            box-shadow: inset 0 0 0 1px #1f1f1f;
        }

        .btn-pink:hover {
            background-color: #ff88dd;
            border-color: #ff88dd;
            cursor: pointer;
            box-shadow: inset 0 0 0 2px #e3279b;
        }

    </style>
</head>
<body>

    <div class="login-container">
        <h2 class="text-center d-flex flex-column justify-content-center align-items-center" 
            style="font-family: 'jsMath', serif;
            font-size: 3rem;
            position: absolute;
            top: 0%;
            left: 50%;
            transform: translate(-50%, -60%);
            width: 15rem; 
            height: 4rem;
            margin: 0;">
            Login
        </h2>
        
        <div style="position: absolute;
                    top: 55%;
                    left: 50%;
                    transform: translate(-50%, -50%);
                    width: 70%;
                    font-family: prompt, sans-serif;
                    font-size: 1.2rem;
                    ">
            
            <form action="/login" method="POST" class="d-flex flex-column w-100">
                
                <% if (typeof error !== 'undefined') { %>
                    <div class="alert alert-danger py-1 text-center w-100" style="border-radius: 20px;">
                        <%= error %>
                    </div>
                <% } %>

                <div class="mb-3 w-100"> <label class="form-label">Email</label>
                    <input type="email" name="email" class="form-control rounded-pill" required>
                </div>
                
                <div class="mb-3 w-100">
                    <label class="form-label">Password</label>
                    <input type="password" name="password" class="form-control rounded-pill" required>
                </div>
                
                <div class="mt-2 text-center">
                    <button type="submit" class="btn rounded-pill px-4 btn-pink">submit</button>
                </div>
            </form>
        </div>
        
    </div>

</body>
</html>